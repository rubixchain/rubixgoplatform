---
phase: "06"
plan: 03
status: complete
note: "Retroactive stub -- executed via quick tasks before plan was written"
---

# Phase 06 Plan 03: c.s.* Removal from Dead-Code Files

## Objective

Replaced all `c.s.*` SQLite storage calls in core/ft.go and core/ft_transfer_optimized.go with `c.w.*` PostgreSQL wallet method calls. These are dead-code files but eliminating `c.s` references removes compile errors.

## Status

**Complete** -- executed 2026-03-20 via quick tasks
