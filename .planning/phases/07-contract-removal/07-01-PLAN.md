---
phase: "07"
plan: 01
status: complete
note: "Retroactive stub -- executed via quick task 260320-5ix (commit d3bd38b)"
---

# Phase 07 Plan 01: Contract Package Elimination

## Objective

Deleted the entire `contract/` package directory. Internalized `ContractTokenInfo` as a core-local type in `core/contract_types.go` (NOT merged with models.TokenInfo -- different field sets, used only in consensus/quorum paths). Created `util.VerifySignature` and `util.SignTransaction` in `util/transaction.go` to replace `contract.VerifySignature`. Updated all 11 files that imported the contract package. `go build ./...` no longer fails on contract package imports.

## Status

**Complete** -- executed 2026-03-20 via quick task 260320-5ix (commit d3bd38b)
