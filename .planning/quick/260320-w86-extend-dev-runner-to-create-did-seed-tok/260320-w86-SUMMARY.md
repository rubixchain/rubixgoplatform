---
phase: quick-260320-w86
plan: 01
subsystem: dev-runner / wallet
tags: [dev-runner, wallet, did, genesis-token, postgresql, seeding]
dependency_graph:
  requires: [core/wallet/did.go, core/wallet/token_chain.go, core/wallet/token.go, core/wallet/post_consensus_persistence.go, types/models/transaction_info.go]
  provides: [core/core.go GetWallet accessor, cmd/dev/main.go DID+token seeding runner]
  affects: [cmd/dev/main.go]
tech_stack:
  added: []
  patterns: [pgtype.Text zero-value for nullable TEXT, int16 casts for DB enum IDs, idempotent genesis seeding via ReadToken pre-check]
key_files:
  created: []
  modified:
    - core/core.go
    - cmd/dev/main.go
decisions:
  - "ReadToken returns non-nil error on not-found; existence check is err==nil && result!=nil"
  - "dev binary artifact added to untracked files — not staged (not in .gitignore yet)"
metrics:
  duration: ~5 minutes
  completed: 2026-03-20
  tasks_completed: 1
  files_modified: 2
---

# Quick Task 260320-w86 Summary

**One-liner:** Added `GetWallet()` accessor to Core and extended the dev runner to exercise full DID-insert + 3 genesis-token-seed + read-back verification through wallet/PostgreSQL.

## What Was Done

### Task 1: Add GetWallet accessor + extend dev runner

**core/core.go** — Added `GetWallet() *wallet.Wallet` accessor immediately after the existing `GetConfig()` method (line 469). No other changes. The `wallet` package was already imported.

**cmd/dev/main.go** — Replaced the blocking `select {}` at the end of main with the full write path:

- **Step A:** `c.GetWallet()` — obtain wallet reference from Core
- **Step B:** `w.CreateOrUpdateDID(...)` — insert `bafybmidtest1234` with `AlgoID = int64(models.GetDidAlgoType(constants.DidAlgo_SECP256K1))` = 1; idempotent via ON CONFLICT UPDATE
- **Step C (loop i=1..3):** For each `QmTestToken00i`:
  - Call `w.ReadToken(tokenID)` — if `err == nil`, token exists, skip
  - Otherwise build `TransactionInfo`, call `wallet.ComputeTransactionID`, `models.SerializeTransactionInfo`, construct `Transactions/Token/TokenChain` structs with proper casts, call `w.PersistGenesisTokenRecord`
- **Step D:** `w.GetFreeRBTTokens("bafybmidtest1234")` — print count and IDs
- **Step E (loop i=1..3):** `w.ReadToken(tokenID)` per token — print TokenID, TokenValue, TokenStatus, DID

**Build result:** `go build ./cmd/dev/` exits 0.

## Verification

```
grep -n "func (c \*Core) GetWallet" core/core.go
# → 469:func (c *Core) GetWallet() *wallet.Wallet {

grep -c "PersistGenesisTokenRecord" cmd/dev/main.go  # → 2 (call + import)
grep -c "GetFreeRBTTokens" cmd/dev/main.go            # → 2 (call + comment)
grep -c "CreateOrUpdateDID" cmd/dev/main.go           # → 2 (call + comment)
go build ./cmd/dev/                                    # → exit 0
```

## Deviations from Plan

None — plan executed exactly as written. The note about `ReadToken` returning an error (not nil) on not-found was consistent with the actual implementation.

## Commits

| Hash | Message |
|------|---------|
| e4dc1ac | feat(260320-w86): add GetWallet accessor + extend dev runner with DID/token seeding |

## Self-Check: PASSED

- `core/core.go` — FOUND (modified, accessor present at line 469)
- `cmd/dev/main.go` — FOUND (extended, 137 lines, all required call sites present)
- commit e4dc1ac — FOUND
